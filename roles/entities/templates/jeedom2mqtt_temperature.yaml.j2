sensor:
  - platform: "mqtt"
    name: "{{ data.entity_id }}_battery"
    state_topic: "rfxcom2mqtt/{{ data.entity_id }}/battery"
    unit_of_measurement: "%"
    value_template: "{{ '{{' }} value {{ '}}' }}"
    device_class: "battery"

  - platform: "mqtt"
    name: "{{ data.entity_id }}_temperature"
    state_topic: "rfxcom2mqtt/{{ data.entity_id }}/temperature"
    unit_of_measurement: "°C"
    value_template: "{{ '{{' }} value {{ '}}' }}"
    device_class: "temperature"

  - platform: "mqtt"
    name: "{{ data.entity_id }}_humidity"
    state_topic: "rfxcom2mqtt/{{ data.entity_id }}/humidity"
    unit_of_measurement: "%"
    value_template: "{{ '{{' }} value {{ '}}' }}"
    device_class: "humidity"

  - platform: "mqtt"
    name: "{{ data.entity_id }}_signal"
    state_topic: "rfxcom2mqtt/{{ data.entity_id }}/signal"
    icon: "mdi:signal"
    unit_of_measurement: "???"
    value_template: "{{ '{{' }} value {{ '}}' }}"
